* root kasutajana
installi - apt-get install git
* tavakasutajana
esmakordselt:

a) seadista - m22ra kasutajanimi, email, editor
git config --global user.name "Ees Perenimi"
git config --global user.email kasutaja@nimi
git config --global core.editor nano

b) loo kataloog, kus hakkad scriptima
mkdir skriptimine

c) sisene antud kausta
cd skriptimine

d) tekita lokaalne repositoorium git systeemi jaoks antud kaustas
git init

remote seadistamine:

a) git remote ad nimi aadress
git remote add skriptimine https://github.com/kasutajanimi/reponimi.git

b) git remote kontroll
git remote -v

p6hikasutamine:

a) pane fail j2lgimisele:
git add failinimi

b) lisa kirjeldus muudatuse kohta
git commit -m "Lyhikirjeldus"

c) lisa kaugrepositooriumi
git push -uf skriptimine master

